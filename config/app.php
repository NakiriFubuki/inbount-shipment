<?php
/**
 * Site URL path on cPanel.
 *
 * Leave empty ('') to auto-detect from the folder where files are installed.
 * Only set base_path if auto-detection is wrong, e.g.:
 *   '/inbound-shipment-system'
 */
return [
    'base_path' => '',
];
