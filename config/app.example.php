<?php
/**
 * Application URL path (cPanel / shared hosting)
 *
 * Copy to app.php. Leave base_path empty for auto-detection (recommended).
 *
 * Examples:
 *   ''                              → auto (folder name in URL = folder on disk)
 *   '/inbound-shipment-system'      → fixed path (no spaces — best for cPanel)
 *   '/Inbound Shipment System'      → only if folder name has spaces on server
 */
return [
    'base_path' => '',
];
